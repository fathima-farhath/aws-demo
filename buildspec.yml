version: 0.2
phases:
  install:
    commands:
      - echo "Installing dependencies"
  build:
    commands:
      - echo "Building the application"
      - cp index.html /var/www/html/
  post_build:
    commands:
      - echo "Build complete"
artifacts:
  files:
    - '**/*'

